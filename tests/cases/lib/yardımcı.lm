# Test yardımcı modülü (koşucu bunu test olarak GÖRMEZ: alt klasörde)
set SÜRÜM = 3

fn selamla(isim):
    return "selam {isim}"

fn topla_hepsi(liste):
    return sum(liste)

set sayac = 0
fn arttir():
    sayac += 1
    return sayac
fn taban_ile(x):
    return SÜRÜM + x
