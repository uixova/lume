use math
say math.lerp(0, 10, 0.5)
say math.PI > 3.14
say len(math), kind(math)
use math as m
say m.clamp(5, 0, 3)
use game: ease
say ease(0.5, "linear")
use strings
say strings.upper("abc"), strings.lower("İĞ")
say math
